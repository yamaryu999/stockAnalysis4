export const metadata = {
  title: "Kabu4 API",
  description: "API service for event-driven stock radar"
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
